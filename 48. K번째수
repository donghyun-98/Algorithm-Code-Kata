def solution(array, commands):
    answer = []
    
    for arr in (commands):
        temp_arr = array[arr[0]-1:arr[1]]
        temp_arr.sort()
        answer.append(temp_arr[arr[2]-1])
    
    return answer
